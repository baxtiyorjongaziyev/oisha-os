{
  "name": "@salescoach/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src",
    "start": "node dist/main.js",
    "test": "node -e \"console.log('worker smoke test placeholder')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@salescoach/shared-types": "workspace:*",
    "bullmq": "^5.61.0",
    "ioredis": "^5.8.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
