## 2026-07-18 - Add clear button to search input\n**Learning:** When adding a clear button ('X') to a search input, ensure it not only resets the search query state but also programmatically returns focus back to the input field (`element.focus()`) to maintain a seamless keyboard navigation flow.\n**Action:** Use `useRef` and a dedicated clear button element with proper ARIA labels inside the search modal.
